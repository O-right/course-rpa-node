# Status

## Current Phase

Course watcher full-course completion verified for `2026春-线性代数` and `道德与法治`, and hardened for recoverable long runs plus mixed resource-card chapters.

## Current Focus

Course watcher has now finished `道德与法治` end to end on the local machine. The first full run `logs/course_morality_full_unbuffered_20260614_093530.log` progressed from `4.4` through `6.4`, then stopped at `6.5 恋爱、婚姻家庭中的道德规范` because the video reported `networkState=3` and no media duration; failure screenshots were saved as `logs/screenshots/20260614_103410_*`. `main.py` now treats this no-source state with visible route options as recoverable by switching to another playback route. Follow-up audits then exposed partially persisted progress at `5.1`, `5.4`, `6.1`, and `6.5`; after tightening video-end detection so the watcher waits for a real ended/near-end-paused state, the final new-session audit `logs/course_morality_final_no_unfinished_audit_20260614_123408.log` exited 0 and reported no unfinished task points. `2025-2026(2) 物理学` exposed a mixed `1课件 / 2视频` resource-card bug at `5.1 物质的微观模型`; `main.py` now iterates visible Chaoxing resource-card tabs before advancing, and `logs/course_physics_5_1_card_fix_20260614_163213.log` verified the PDF card was handled before switching to the video card and watching it to 100%. Assignment tester remains in a temporary experiment phase where the real Chaoxing assignment host hard-block may stay relaxed by explicit user direction and should be restored later. Do not persist replacement tokens, cookies, credentials, or `.env` values in project files. The project is hosted on GitHub at `https://github.com/O-right/course-rpa-node`; local `main` tracks `origin/main`.

## Completed

- Project rules and kickoff docs created.
- Dependency file created.
- Core automation script drafted.
- Python dependencies installed from `requirements.txt`.
- Playwright Chromium installed.
- `python -m py_compile main.py` passed.
- Playwright import smoke test passed.
- `import main` smoke test passed.
- Chaoxing login URL, course keyword, and login selectors configured.
- `run_chaoxing.ps1` added; it now uses local `.env` automatically when present.
- Authenticated probing confirmed the target course appears in the Chaoxing course iframe.
- Course-page probing found the `章节` tab and an online learning commitment dialog.
- `main.py` now switches to the course popup page, searches page frames, and attempts automatic commitment confirmation when present.
- `README.md` added with setup and run instructions.
- Fixed Playwright Python `Locator.first` usage that caused `TypeError: 'Locator' object is not callable`.
- Locator helper smoke test passed for `#phone`, `#pwd`, and `#loginBtn`.
- Fixed course-card lookup timing by polling current page frames until `lookup_timeout_ms` instead of checking the frame list only once.
- Delayed iframe lookup smoke test passed for the `“四史”专题课` course selector.
- Saved local ignored `.env` credentials for automatic login on this machine.
- Added `python-dotenv` so `main.py` reads `.env` directly.
- Tightened chapter selection to `.chapter_item[onclick*='toOld']` so the script clicks a real task-point chapter rather than a non-navigating title.
- Authenticated smoke test passed through login, course open, chapter open, video detection, and setting `playbackRate` to `2`.
- Added `assignment_tester.py` as an independent assignment/quiz interaction robustness tester with an intended dry-run safety model.
- Added `requests` dependency for local OpenAI-compatible API calls.
- Verified `ask_ai_brain()` response parsing with a fake API response.
- Verified local DOM extraction for question text, options, type, and controls.
- Configured assignment tester AI endpoint from `.env`: `https://api.2070814.xyz/v1/chat/completions`, model `gpt-5.5`.
- Real AI endpoint smoke test now passes from this machine: `ask_ai_brain()` returned `["A"]` for a neutral connectivity test question.
- Confirmed Chaoxing login DOM in headless Playwright: username `#phone`, password `#pwd`, login button `#loginBtn`.
- Updated `assignment_tester.py` default login URL and selectors to Chaoxing.
- Saved `ASSIGNMENT_LOGIN_URL`, `ASSIGNMENT_USERNAME`, and `ASSIGNMENT_PASSWORD` in local ignored `.env`.
- Verified both `main.py` and `assignment_tester.py` load the Chaoxing login URL and locate the login selectors on the live page.
- Enhanced `assignment_tester.py` to navigate from Chaoxing home to the configured course, click the course sidebar `作业` tab, and search assignment content across page frames and `frame_locator`.
- Probed the live course assignment page: `#nav_7059` opens `mooc2/work/list...`; the current course reports `暂无作业`.
- Added Chaoxing-style question extraction support for `.Py_tk` question stems and `.Py_answer` option lists inside iframes.
- Verified local iframe extraction for `.Py_tk` and `.Py_answer li` with radio controls.
- Verified live login -> course -> assignment tab flow; it returns gracefully because no unsubmitted homework exists in the current course.
- Refactored `assignment_tester.py` into background monitor entrypoint with `run_monitor()` and `scan_inbox()`.
- Added `server_mode`, `headless`, `scan_interval_seconds`, `max_scan_rounds`, `inbox_keywords`, and `inbox_unhandled_keywords` config.
- Added Inbox candidate scanning, keyword/status matching, candidate navigation, and timestamped scan logs.
- Added direct `ASSIGNMENT_INBOX_URL` support plus fallback to configured course `作业` tab when no Inbox entry is visible.
- Verified local Inbox scan smoke test with one matching candidate.
- Verified one live monitor round: login succeeded, Inbox entry was not visible, fallback reached course `作业`, and no pending homework was found.
- Updated Inbox scanning to match confirmed Chaoxing DOM: iterate `li.dataBody_item`, read/click `a.notice_title`, and filter titles containing `作业：`.
- Verified local Chaoxing Inbox DOM smoke test: only `li.dataBody_item > a.notice_title` with `作业：` is selected.
- Fixed `main.py` next-chapter handling by adding the real visible Chaoxing control `#prevNextFocusNext`; verified click navigates from `chapterId=1151598212` to `chapterId=1151598213`.
- Hardened `main.py` next-chapter handling with direct `#prevNextFocusNext` click and onclick fallback.
- Verified `CourseAutoTester.click_next_chapter()` integration: login, open first lesson URL, click next, land on `chapterId=1151598213`, and detect video.
- Rebuilt `assignment_tester.py` as a pure Inbox monitor: removed course-name/course-list/course-assignment fallback logic and all course navigation.
- `assignment_tester.py` now reads assignment-scoped AI variables (`ASSIGNMENT_AI_API_URL`, `ASSIGNMENT_AI_API_KEY`, `ASSIGNMENT_AI_MODEL`) from `.env`.
- Verified live pure Inbox one-round monitor: login -> forced Inbox URL -> 0 `作业：` candidates -> logs `扫描完毕，暂无作业，准备休眠`; no course page was visited.
- Fixed an Inbox navigation race after login by waiting for `i.chaoxing.com` and retrying forced Inbox navigation if the first `page.goto()` is aborted.
- Re-ran visible one-round monitor: login succeeded, forced Inbox URL opened, scan found 0 `作业：` candidates, and exited cleanly.
- Fixed live Inbox loading by clicking the `收件箱` navigation entry after the forced `i.chaoxing.com/base` shell URL loads.
- Updated Inbox matching to handle both `作业：` and real DOM `作业:` titles, and to extract `.openNotice[data-url]` detail links.
- Added notice-detail attachment handling: `assignment_tester.py` reads `window.att_web.url` / `window.screenOpenUrl` from the attachment iframe and opens the real `intoexamorwork` URL.
- Added Chaoxing `dowork` question selectors: `.questionLi`, `.mark_name`, and `.answerBg[role=radio|checkbox]`.
- Verified visible one-round Inbox flow against the read `作业:《思想道德与法治》2023版第三章` notice: scan found 8 assignment candidates, limited processing to the first, opened the notice detail, followed the assignment attachment, reached `mooc2/work/dowork`, extracted a single-choice question with options `A-D`, and completed dry-run mapping without submission.
- Added multi-question processing for assignment test pages: `process_all_questions()` enumerates `.questionLi` containers up to `max_questions`, extracts each stem/options/type, maps answers, and defers next/submit until the page-level question loop completes.
- Added real-site safety hardening originally intended to block Chaoxing assignment hosts from live AI calls and answer clicks even if a config flag is changed; current experiment stance supersedes that as an immediate blocker.
- Optimized `.answerBg` option text extraction to avoid slow label-selector timeouts.
- Verified headless dry-run against the live `dowork` page: 10 question containers were detected; 1-5 parsed as single choice, 6-10 parsed as multiple choice; no AI request or answer click was made on the real host.
- Verified a focused two-question dry-run after the extraction optimization: both questions parsed and mapped quickly, with `Live AI disabled on real assignment host` logged.
- Added text-answer detection for non-choice questions: when a question container has no choice options, `assignment_tester.py` now looks for `input[type=text]` or `textarea` and marks the question as `text`.
- Updated AI payload shape for authorized staging text questions to include `question_type`, and accepts either `{"answer": "文本"}` or `{"answer": ["文本"]}` responses.
- Added dry-run text fill path: logs `[探测到简答题] 准备填入内容：...`; real `.fill()` still requires actions to be allowed.
- Verified local DOM smoke test with one `textarea`简答题 and one `input[type=text]`填空题; both parsed as `text` and logged the dry-run fill message without writing.
- Verified mocked API parsing for a text answer response without making a network request.
- Detected assignment tester safety configuration drift during repo resume: current code does not match the intended safe defaults documented in the design notes.
- Hardened staging-only submit flow: submit selector now includes Chaoxing-style `.Btn_blue_1:has-text('提交')` and `a.btnSubmit`, scrolls the button into view, waits before/after clicking, and raises a `RuntimeError` if no submit button is found when submission is allowed.
- Added JS dialog handling with `page.on("dialog", ...)` and custom confirmation handling for visible confirm buttons such as `确定` / `确认`.
- Added explicit submit debug logs: `[Action] 正在定位提交按钮...`, `[Action] 点击提交...`, `[Action] 处理确认弹窗...`, and completion logging.
- Verified local staging fixtures for submit hardening: one custom confirmation dialog and one native `alert('确认提交吗')` both completed successfully.
- Re-applied submit double-confirmation handling in `assignment_tester.py`: submission now ensures the native dialog listener is active immediately before clicking, waits 2 seconds for a custom confirmation, and includes `.bluebtn`, `确认提交`, `确定`, and `确认` selectors.
- Sped up assignment interaction timing: normal click/fill delays are now `0.8-1.8s`, and next/submit reading delays are now `3-8s`.
- Documented the assignment tester drift instead of changing code: under the original staging-only design it needed a safety pass before real assignment use; as of 2026-05-31 this is temporarily accepted for experimentation.
- Fixed a second submit-confirmation failure mode: custom confirmation handling now supports up to 3 confirmation layers and can click popup buttons labeled `提交` as well as `确认提交` / `确定` / `确认`.
- Removed the invalid Playwright `.filter(state="visible")` usage from the submit confirmation path and replaced it with explicit visible/enabled locator scanning across the page and frames.
- Optimized `main.py` for unattended course watching: core runtime settings now read from `CX_*` environment variables, including headless mode, slow motion, playback rate, max chapters, timeouts, and screenshot directory.
- Added command-line course selection to `main.py` and `run_chaoxing.ps1`: use `.\run_chaoxing.ps1 --course "课程名称关键词"` for a one-off target course.
- Updated course watcher completion handling: after a video finishes, missing or disabled next-section controls are treated as normal end-of-course completion when `CX_STOP_WHEN_NO_NEXT=true`.
- Added learning-task processing inside `main.py`: each current task page processes all detected `<video>` elements in sequence; no-video pages are treated as courseware/non-video tasks with quick open/return handling.
- Added `--fast-actions` and `CX_MIN_ACTION_DELAY_SECONDS` / `CX_MAX_ACTION_DELAY_SECONDS` so authorized test runs do not wait 2-5 seconds between every click/input.
- Fixed Chaoxing next-task detection for same-URL transitions by accepting page-content signature changes as successful advancement.
- Added playback recovery in `main.py`: when video pauses, playback rate drifts, or progress stalls for configured polls, the script re-applies mute/playbackRate and calls `play()` again.
- Hardened Chaoxing next-section handling: `#prevNextFocusNext` is only used when visible and not disabled; after click or onclick fallback, URL must change before the script treats the next-section transition as successful.
- Added failure screenshots for login/course/chapter/video/next-section failures and unexpected exceptions, saved under `logs/screenshots` by default.
- Updated `README.md` with course watcher runtime settings and recovery behavior.
- Verified `main.py` syntax, environment-variable config parsing, and a local `#prevNextFocusNext` navigation smoke test.
- Updated `README.md`, `docs/STATUS.md`, `docs/TASKS.md`, and `docs/DECISIONS.md` to reflect that assignment tester safety defaults are out of sync with the original staging-only model.
- Verified first course-selection slice locally: `python -m py_compile main.py assignment_tester.py`, `python main.py --help`, `.\run_chaoxing.ps1 --help`, CLI config override parsing, and conflicting `--headless --headed` rejection.
- Live-tested `中国近现代史纲要` with `python -u main.py --course "中国近现代史纲要" --max-chapters 1 --headless --fast-actions` while using the same arguments supported by `run_chaoxing.ps1`: login succeeded, course opened, one task page with 1 video was processed, the 52.5s video reached 100% at 2x speed, and the next-task click was accepted via same-URL page-content change.
- Live-tested `中国近现代史纲要` with `python -u main.py --course "中国近现代史纲要" --max-chapters 5 --headless --fast-actions`: completed 5 learning task points, processed 8 videos to 100% at 2x speed, handled one no-video task with short wait, handled one courseware/non-video task by opening the courseware entry and continuing, accepted same-URL page-content transitions, and used the onclick fallback when a completion overlay intercepted the normal next-button click. Log: `logs/live_china_history_5tasks_20260427_134618.out.log`; stderr was empty.
- 2026-06-09 verified `2026春-线性代数` full-course completion on PID `42896`: the watcher processed the final task point, followed the last no-next path, saved `logs/screenshots/20260609_182149_no_next_chapter_in_list.png`, and exited normally with `课程 [线性代数] 已处理到最后一节，自动看课流程完成。`
- 2026-04-28 OpenClaw Feishu-triggered live run reached this runner with `python -u main.py --course 中国近现代史纲要 --headless --fast-actions`. It logged in, opened the course, processed 3 learning task points, observed 2x video progress, and saved screenshots when the third video progress locator timed out near completion.
- Fixed `main.py` so failure paths return process exit code 1 instead of silent process success, and video progress reads retry by re-locating the indexed video before failing.
- 2026-04-28 fresh OpenClaw Feishu-triggered run after the stale-locator fix passed the previous failure point: task point 3 video 3/3 completed, task points 4 and 5 advanced, and task point 6 videos 1-5 completed.
- That fresh run was intentionally stopped from OpenClaw when task point 6 video 6/7 reported duration `7238.0s`; this was a quality-test run, not a full-course completion run.
- Created tutai deployment documentation at `D:\infra-vault\servers\tutai\course-rpa-node.md`.
- Created local tutai deployment archive at `dist\course-rpa-node-tutai-20260427.zip`; archive contents were checked and exclude `.env`, `logs`, and `__pycache__`.
- Verified tutai SSH host key fingerprint and added it to local `known_hosts`.
- Configured dedicated SSH key access for tutai; non-interactive SSH now works for `administrator@47.110.36.32`.
- Installed the deployment archive on tutai under `D:\services\course-rpa-node`.
- Installed Python dependencies on tutai. Playwright `1.48.0` installed successfully but its Chromium build cannot run on Windows Server 2012 R2, so the tutai runtime was downgraded to Playwright `1.29.1` with Chromium `109.0.5414.46`.
- Verified tutai after the downgrade: `python -m py_compile main.py assignment_tester.py`, `python main.py --help`, and a headless Playwright Chromium smoke test all passed.
- Synced the local ignored `.env` to tutai `D:\services\course-rpa-node\.env` with UTF-8 no BOM; contents were not printed or written to docs.
- Ran tutai `中国近现代史纲要 --max-chapters 1 --headless --fast-actions` smoke. The script reached the login navigation step, but Chaoxing returned `net::ERR_TOO_MANY_REDIRECTS` and saved `logs\screenshots\20260427_164408_login_failed.png`.
- Probed the tutai Chaoxing login redirect chain without credentials. `passport2.chaoxing.com/login...` redirects to `https://passport2-api.chaoxing.com/views/error/passport403.html`, and that 403 page redirects to itself. This indicates a Chaoxing access block for tutai's cloud-server environment/IP, not a selector or credential failure.
- Removed the tutai deployment directory `D:\services\course-rpa-node`; this also removed the synced remote `.env` and remote logs/screenshots. Confirmed the previous SFTP upload location `D:\qampp\htdocs\course-rpa-node-tutai-20260427.zip` is absent.
- Added system browser launch support to `main.py` for slow Playwright browser downloads: use `--browser-channel chrome`, `--browser-channel msedge`, `CX_BROWSER_CHANNEL`, or `CX_BROWSER_EXECUTABLE_PATH`.
- Updated docs on 2026-05-31 to record the current assignment tester experiment stance: the hard-block drift is intentional for now, not the next blocker, and must be restored after the experiment phase.
- Optimized assignment tester timing for the temporary experiment phase: default click/fill delays are now `0.05-0.15s`, next/submit reading delays are `0.1-0.3s`, networkidle waits are disabled by default, submit confirmation uses short polling, and incomplete-answer blocking is configurable instead of sleeping for 7200 seconds by default.
- Optimized course watcher timing: video completion polling now wakes early based on remaining duration and playback rate, and `--fast-actions` also caps progress polling at 3 seconds and non-video courseware hold time at 0.2 seconds.
- Initialized local Git repository, created GitHub repository `O-right/course-rpa-node`, uploaded source/docs while excluding `.env`, `logs/`, `dist/`, `__pycache__/`, and synced local `main` with `origin/main` after normal GitHub HTTPS connectivity recovered.
- Inspected remote `glm` LXC `chat2api`: `chat2api.service`, `cloudflared-api-tunnel.service`, and `mihomo.service` are running; `api.2070814.xyz` routes through Cloudflare Tunnel to `127.0.0.1:5005`; `/v1/models` returns 200 locally and publicly.
- Diagnosed current assignment AI endpoint degradation: authenticated `/v1/chat/completions` returns 500; `chat2api` logs show `Request token is empty, use no-auth 3.5`, `Unusual activity has been detected`, and intermittent `SSL_ERROR_SYSCALL` to `chatgpt.com`; `chat2api-session-refresh.service` is failed and `data/token.txt` is effectively empty.
- Restored `glm/chat2api` health by switching `mihomo` AI traffic to the user-provided `ProxyPorts-US-ISP` chained proxy, refreshing the ChatGPT session access token, and verifying authenticated `gpt-5.5` chat completions return HTTP 200.
- Verified `gpt-5.5` model routing: public and local `/v1/chat/completions` return `model=gpt-5.5`, while service logs show `Model mapping: gpt-5.5 -> gpt-5-5-thinking` and `Response Model: gpt-5-5-thinking`.
- Ran one authorized real assignment experiment against `作业:《思想道德与法治》2023版第五章第一节`: scanned Inbox via direct `notice.chaoxing.com` URL, opened the notice attachment, answered 10 questions, submitted, and verified the result page showed 90/100 with question 10 missed because the first AI decision selected `ABC` instead of the correct `ABCD`.
- Improved `assignment_tester.py` AI auditability and multi-select reliability: raw AI responses and normalized answers are logged, multiple-choice questions receive a second AI review pass by default, normalized choice letters are de-duplicated and ordered, and a pre-submit answer summary is logged.
- Added screenshot-based image-question support to `assignment_tester.py`: question containers with `img`, `svg`, `canvas`, or CSS `background-image` are captured as in-memory PNG data URLs and sent to the AI using multimodal `image_url` message parts. Pure text questions keep the existing text-only payload.
- Verified image-question plumbing with a local Playwright fixture and verified `api.2070814.xyz` accepts a multimodal `gpt-5.5` payload containing a data URL image.
- Added low-confidence halt support to `assignment_tester.py`: AI responses now request/parse `confidence`, default minimum confidence is `0.75`, missing confidence is treated as low confidence, and low-confidence results stop before next/submit while optionally holding the visible browser for manual inspection.
- Added guarded real-run stops for unanswerable questions, incomplete answer mapping, and low post-submit score. The low-score threshold defaults to `80` via `ASSIGNMENT_MIN_ACCEPTABLE_SCORE`.
- Ran a guarded live Inbox scan on 2026-06-01 with real login, live AI enabled, image support enabled, low-confidence halt enabled, unanswerable halt enabled, and low-score halt enabled. The configured `i.chaoxing.com` Inbox entry loaded and reported 0 assignment candidates, so no assignment was answered or submitted in that run. Log: `logs/assignment_live_guarded_20260601_123823.log`.
- Fixed the Inbox scan miss by reading the shell menu's `dataurl` for the direct `notice.chaoxing.com` Inbox URL and by accepting both `data-url` and real DOM `dataurl` on `.openNotice` detail links.
- Re-ran the guarded live flow after the scan fix. It found 9 assignment candidates, skipped the already-viewable/completed first candidate, opened `作业:《2025-2026-2细胞生物学》作业9-细胞C`, answered/clicked questions 1-52, then stopped before submit on question 53 because the AI confidence fell below the `0.75` threshold and the DOM option extraction looked malformed. Log: `logs/assignment_live_guarded_fixed_20260601_125042.log`.
- Added an excessive-option-count guard: if a question yields more than `ASSIGNMENT_MAX_OPTIONS_PER_QUESTION` options, it is treated as unanswerable and the guarded run stops before submit.
- Diagnosed question 53 as Chaoxing `共用选项题`: one shared A-E option block plus four sub-questions, with clickable controls under `.B-answer-ct .B-answerCon span`. Added structured shared-option extraction and per-sub-question clicking.
- Verified question 53 and 54 shared-option parsing against the live page without submission; both extract 5 shared options, 4 sub-questions, and 20 controls. Live AI returned ordered answers for Q53 (`B,C,A,E`, confidence 0.92) and Q54 (`C,A,C,A`, confidence 0.90).
- Re-ran the guarded live flow after the shared-option fix. It did not reach Q53 because the low-confidence guard stopped earlier on question 27 (`confidence=0.68 < 0.75`), before submit. Log: `logs/assignment_live_shared_fixed_20260601_161005.log`.
- Added an enhanced-review accuracy ladder for assignment answers: high-confidence answers proceed, medium-confidence answers get repeated AI review and consensus scoring, stable medium-confidence answers are recorded against a pre-submit risk budget, transient AI request failures and flaky login navigation are retried, and unstable or very low-confidence answers still halt before submit.
- Hardened the assignment question cap: `ASSIGNMENT_MAX_QUESTIONS` now defaults to 120, container discovery keeps one extra question as a cap sentinel, and hitting the cap stops before submit so a long assignment is not partially submitted.
- Fixed dry-run shared-option mapping: when live AI is disabled on a real assignment host, `assignment_tester.py` now repeats the deterministic mock option for each shared-option sub-question so dry-run control mapping does not falsely halt on `共用选项题`.
- Ran a guarded real-site dry-run scan on 2026-06-01 with live AI and submission disabled. It found 9 Inbox assignment candidates, skipped completed `work/view` pages, parsed/mapped several `dowork` pages including shared-option questions, and exited at `ASSIGNMENT_MAX_SCAN_ROUNDS=1` without submitting. Log: `logs/assignment_live_safe_scan_all_after_shared_dryrun_fix_20260601_215849.log`.
- Continued the authorized real assignment run on 2026-06-01 after user confirmation that the pending assignments are test-created. The run submitted at least one biology assignment and later reached an English image/fill-in assignment before stopping on AI/backend failures rather than a no-work scan.
- Hardened image and fill-in handling in `assignment_tester.py`: browser contexts now default to `ASSIGNMENT_DEVICE_SCALE_FACTOR=2.0`, text questions can map multiple blanks to ordered answer arrays, hidden textareas fall back to JS value assignment, and Chaoxing prompt-page score parsing handles `0分 提交时间...`.
- Diagnosed the latest `glm/chat2api` outage after a user-provided ChatGPT session-token update. `chat2api.service`, `cloudflared-api-tunnel.service`, and `mihomo.service` are active, but the new `__Secure-next-auth.session-token` value, including the user-provided `.0/.1` split, only returns `WARNING_BANNER` from `chatgpt.com/api/auth/session` and does not yield an `accessToken`; local `assignment_tester.ask_ai_brain_decision()` still fails with HTTP 401.
- Checked fallback AI options during the outage: the local SiliconFlow key can list models but chat completions fail for insufficient balance, and the local Anthropic-compatible endpoint configured in environment variables is not listening.
- Restored `glm/chat2api` on 2026-06-02 by installing a fresh ChatGPT `accessToken`, replacing `/home/ubuntu/proxy/config.yaml` with the user-provided chained proxy config for the local Mihomo service, and restarting `mihomo.service` plus `chat2api.service`. Local `assignment_tester.ask_ai_brain_decision('1+1=?')` smoke returned `['B']` with confidence `1.0`.
- Ran a real submit-enabled assignment scan on 2026-06-02 with user confirmation that the Inbox assignments are test-created. The scan found 8 candidates, skipped completed/view-only candidates, opened one `dowork` assignment, answered and submitted 10 questions, then continued to an English image/fill-in assignment and stopped before submit because the AI returned non-JSON text with no usable answer. Log: `logs/assignment_real_submit_20260602_145815.log`.
- Fixed the assignment AI parser and normalizer after the real scan exposed two failure modes: `chat2api` can wrap JSON in Markdown/citation/tool-call text, and the AI can return an option value such as `"6"` instead of the option letter. `assignment_tester.py` now extracts the first JSON object from noisy responses and maps unique option text/value matches back to A/B/C letters.
- Re-ran a bounded real submit-enabled scan after the parser fix. The log shows a fenced ` ```web ... ``` ` AI response was parsed and normalized to `['A']`, the earlier unanswerable English image/fill-in candidate was skipped because `ASSIGNMENT_SKIP_UNANSWERABLE_CANDIDATE=true`, and a later 44-question candidate reached submit handling after all 44 questions were selected/mapped. The parent shell timed out at 15 minutes, so the final submit/result status for that last candidate is not fully logged. Log: `logs/assignment_real_submit_after_parsefix_20260602_182555.log`.
- Fixed `main.py` course lookup for the `“四史”专题课` card: course keyword matching now tries quote variants and common course-name suffix fallbacks, so `四史专题课` can fall back to `四史` when the rendered card title contains quote characters.
- 2026-06-02 latest result-page inspection for `作业:《2026春-线性代数》2026.0528作业` opened the `prompt` page, followed `查看详情` to `work/view`, and identified the 70/100 low-score causes: Q3 submitted `B` but correct was `A`, Q5 submitted `B` but correct was `A`, and Q9 submitted `错` but correct was `对`. This confirmed the low score was answer quality, not click mapping or submit completion.
- Hardened assignment AI review after the 70/100 result: math-like, true/false, and media-backed questions now enter enhanced review even when the primary answer reports high confidence; high-risk review defaults to stricter consensus and can halt instead of submitting unstable high-confidence answers. Media payloads now include explicit `media-node-N` labels so image order can be tied back to option order.
- Latest guarded real run after the high-risk review patch skipped completed/notice-only candidates and reached `作业:《英语02》课后作业Book2 Unit 5`; it filled four text/image fill-in questions but stopped on submit completion verification while still on `dowork`. Probe evidence showed Chaoxing raised `您还有未做完的填空题，确认提交吗？`, because hidden UEditor-backed text inputs were not synced through `UE.getEditor(...).sync()`.
- Minimal follow-up fix applied: hidden text-answer JS fallback now updates matching UEditor instances with `setContent()` and `sync()`, and submit confirmation wait defaults to 12 seconds to cover delayed Chaoxing validation popups.
- A guarded real run after the UEditor sync fix verified the final requested stop condition. It found 8 Inbox candidates, skipped completed/view-only candidates and one notice-only reminder, submitted `作业:《2025-2026-2细胞生物学》作业8` with score `92.4/100`, then skipped the remaining completed candidates and exited with `ASSIGNMENT_EXIT_WHEN_NO_UNSUBMITTED=true; all scanned assignment candidates are completed/closed.` Log: `logs/assignment_real_until_empty_20260602_221715.log`.
- 2026-06-03 real scan rerun verified the requested stop condition without needing to answer a new `dowork` page. Because the remote `glm/chat2api` stored token was invalidated, the run used a user-provided ChatGPT access token as the per-process `ASSIGNMENT_AI_API_KEY`; assignment AI smoke returned `['B']` for `1+1=?` before the scan. The scan found 8 Inbox candidates, classified 7 as `completed_or_closed` and 1 as `notice_only`, and exited normally with `ASSIGNMENT_EXIT_WHEN_NO_UNSUBMITTED=true; all scanned assignment candidates are completed/closed.` Log: `logs/assignment_real_until_empty_20260603_000824.log`.
- 2026-06-03 incident review for `作业:《2025-2026-2细胞生物学》作业9-细胞C` confirmed the result page is `0分` with blank `我的答案`. The responsible run is `logs/assignment_live_full_two_after_attachment_retry_20260601_221857.log`: it clicked 54 answers and submitted at 22:39, but the old code treated click success as answer persistence and did not verify selected state before submit.
- 2026-06-03 incident review for `作业:《英语02》课后作业Book2 Unit 5` confirmed score `44.4/100`; Q2 and Q4 were `0分`. The result page showed hidden fill-in answers were cyclically shifted relative to the logged intended answers, and the old high-risk text adjudication path overrode multi-sample disagreement. `assignment_tester.py` now sorts hidden text controls by visible editor position, verifies choice/shared-option selected state after clicks, and keeps high-risk text adjudication from overriding review disagreement.
- Fixed course watcher false-completion handling: when no next control or catalog entry is available, `main.py` now waits briefly for completion-state refresh and fails with a `no_next_but_incomplete` screenshot if the current page/catalog still exposes unfinished-task markers. `video_locators()` also scans the main page in addition to frames.
- Fixed the latest `“四史”专题课 --chapter "6.1"` failure: the watcher now prefers real course-card links such as `.course-info a.color1`, falls back to direct href navigation if target `_blank` does not open a popup, and scrolls the chapter catalog while searching for later chapter keywords.
- Live-supervised `“四史”专题课` 6.1 on 2026-06-03. The watcher opened the course, clicked 6.1, waited for the nested video iframe, played the 5495.2s video at 2x from 58.5% through 100%, and the platform screenshot showed `任务点已完成` plus a green check on 6.1. A follow-up probe verified `incomplete_learning_signals()` returns `[]` and the final no-next path returns `completed`; a course-list probe then showed `任务点进度: 8/8 100% “四史”专题课`.
- Refreshed the remote `glm/chat2api` token on 2026-06-06 by installing a user-provided access token into `/home/ubuntu/chat2api/data/token.txt`, backing up the previous file, clearing `error_token.txt` / `refresh_map.json`, and restarting `chat2api.service`. Local loopback and public `api.2070814.xyz` authenticated chat-completion smokes returned `OK`; local `assignment_tester.ask_ai_brain_decision()` returned `['B']` with confidence `1.0`.
- Updated `/home/ubuntu/chat2api/data/session_cookie.txt` on 2026-06-06 with a user-provided raw `sessionToken`. The remote `refresh-access-token-from-session.sh` successfully exchanged it through `chatgpt.com/api/auth/session`, wrote a valid JWT to `token.txt`, restarted `chat2api.service`, and passed its local smoke test. The script is also hardened so an empty or malformed fetched token cannot overwrite a valid `token.txt`.
- Synced repository handoff docs after the assignment, course-watcher, and `chat2api` operational work. Repo maintenance must not write API tokens into `.env`, docs, logs, commits, or test fixtures.
- Fixed `assignment_tester.py` so same-URL Chaoxing `dowork` pages containing submitted-result markers such as `我的答案` / `正确答案` are treated as submission/result evidence instead of causing `Submit completion was not verified after click`.
- Added low-score retry marker handling: when a result page contains `未达到及格线` plus a retry marker but no numeric score, the low-score guard halts instead of continuing silently.
- Reran the real Inbox scan on 2026-06-06. Latest evidence `logs/assignment_real_until_empty_20260606_163727.err.log` shows 10 candidates, recognizes the first candidate as an already-submitted result page, and stops on the low-score/retry guard without traceback.
- Added visible-correct-answer retry support for Chaoxing result pages: when a retry page exposes `正确答案`, the tester can parse the visible letters, apply exact choice state, and verify the final selected state before submit.
- Reworked the first low-score retry candidate `作业:《思想道德与法治》2023版第五章第二节` using the visible `正确答案` list; submitted after review and verified score `100/100`. Evidence: `logs/assignment_submit_visible_correct_20260606_164858.*`.
- Submitted `作业:《2025-2026-2细胞生物学》作业11` only after manual pre-submit review corrected Q7 from `D` to `B` and all 24 selected states matched the fixed answer list. Verified score `96/100`. Evidence: `logs/assignment_submit_manual_candidate2_20260606_170111.*`.
- Fixed malformed AI JSON fallback in `assignment_tester.py`: when an AI response contains a recoverable `"answer"` field but malformed surrounding JSON or an unusable confidence value, the tester now preserves the normalized answer and forces a low-confidence guard reason instead of silently discarding the answer or trusting the bad confidence.
- Added durable manual-review enforcement in `assignment_tester.py`: `ASSIGNMENT_REQUIRE_MANUAL_REVIEW_BEFORE_SUBMIT=true` writes a pre-submit review JSON and halts; `ASSIGNMENT_REVIEWED_ANSWER_FILE` applies a fixed reviewed answer map with assignment-title guarding instead of re-querying AI.
- Submitted `作业:《2025-2026-2细胞生物学》作业10` from `logs/reviews/biology10_reviewed_20260606_manual.json` after manual review corrected Q83 from `A/D/E` to `D/E`. The submit log loaded 87 reviewed answers, applied 87/87, and verified score `95.5/100`. Evidence: `logs/assignment_submit_biology10_reviewed_20260606_185240.*`.
- Ran the final no-unsubmitted scan on 2026-06-06. It found 10 Inbox assignment candidates, classified 9 as `completed_or_closed` and 1 as `notice_only`, then exited with `ASSIGNMENT_EXIT_WHEN_NO_UNSUBMITTED=true; all scanned assignment candidates are completed/closed.` Evidence: `logs/assignment_until_empty_final_20260606_190457.*`.
- Re-ran a fresh goal-audit Inbox scan on 2026-06-06 with `ASSIGNMENT_REQUIRE_MANUAL_REVIEW_BEFORE_SUBMIT=true`, `ASSIGNMENT_REVIEWED_ANSWER_FILE=` empty, `ASSIGNMENT_MAX_CANDIDATES_PER_ROUND=0`, and `ASSIGNMENT_EXIT_WHEN_NO_UNSUBMITTED=true`. It again found 10 candidates, classified 9 as `completed_or_closed` and 1 as `notice_only`, generated no new pre-submit review report, and exited with code 0 on the no-unsubmitted stop condition. Evidence: `logs/assignment_goal_audit_20260606_191447.*`.
- Fixed a remaining multi-blank text ordering bug in `assignment_tester.py`: after visible-position sorting, `apply_text_answers()` was still sorting keys lexicographically, so `__text_10__` could be applied before `__text_2__`. Text answer keys now sort by numeric blank index before filling.
- Fixed the `2026春-线性代数` course watcher stall at `2.4 克拉默法则`: recovery now clicks Video.js player controls, re-locates videos after iframe recreation, and skips forcing 2x when the task page says `不可倍速`.
- Verified the fixed `2.4` path on the live course. `logs/course_linear_2_4_resume_20260608_205021.out.log` shows continuous playback at the platform-allowed 1x from 14.9s to 333.7/334.2s, then a successful next-unit click.
- Added and live-verified a current-task skip guard to `main.py`: before processing videos on a learning page, the watcher reads the active catalog item and skips playback only when that current task point is explicitly marked complete. The completion check is scoped to the current catalog item so sibling `未完成` text does not pollute the result. A `道德与法治` retry against completed chapter `4.2` skipped playback and advanced without replaying the 723s video.
- Fixed the `道德与法治` `6.5` video-source failure path: when a video is truly no-source (`networkState=3`, no duration/current time/ready metadata) and the page exposes alternate route controls such as `公网1` / `公网2`, the watcher clicks an unselected route and re-waits for media metadata before failing.
- Completed `道德与法治` after the route-switch hardening. Evidence: failed full run `logs/course_morality_full_unbuffered_20260614_093530.log`, failure screenshots `logs/screenshots/20260614_103410_*`, and successful resume run `logs/course_morality_resume_6_5_20260614_112503.log`.
- Fixed early video-completion persistence in `main.py`: the watcher no longer exits as soon as playback is within one second of `duration`; it now waits for `ended` or near-end paused state, then gives the platform a short completion-settle window controlled by `CX_VIDEO_COMPLETION_SETTLE_SECONDS`.
- Re-ran `道德与法治` after the persistence fix. `logs/course_morality_end_persist_fix_20260614_123048.log` watched `6.5` until `702.4/702.4s` with `paused=True`, and `logs/course_morality_final_no_unfinished_audit_20260614_123408.log` exited 0 in a new session without finding unfinished task points.
- Fixed mixed resource-card handling for Chaoxing course chapters such as `2025-2026(2) 物理学` `5.1 物质的微观模型`, where the page shows `1课件 / 2视频` but initially loads only the PDF card (`num=0`). The watcher now iterates visible `#prev_tab` / `.prev_list` card tabs and re-detects videos after switching cards.
- Verified the mixed card fix on the live `5.1` page. `logs/course_physics_5_1_card_fix_20260614_163213.log` shows `当前学习页检测到多个资源卡: 1课件 / 2视频`, then `已点击: 学习资源卡: 2视频`, then the `116.4s` video reached `100.0%` with `paused=True`.

## Next Action

- `2026春-线性代数` and `道德与法治` full-course completion are verified, so no active course-watcher run remains for those courses. Future validation can target another course or a regression path if needed.
- For `2025-2026(2) 物理学`, rerun the watcher from the current remaining unfinished point with `python -u main.py --course "2025-2026(2) 物理学" --max-chapters 200 --headless --fast-actions --browser-channel chrome`; mixed `课件/视频` cards are now handled, but full-course completion after this fix still needs a user or local long run.
- If duplicate-watching behavior needs real-site proof, run a bounded live course watcher check on a course with mixed completed/unfinished task points and confirm the log prints `当前任务点已在章节目录中标记完成，跳过视频播放` before advancing.
- The current Inbox assignment goal is verified by the latest uncapped goal-audit scan. Future assignment runs should start from a fresh bounded scan and keep the manual-review gate enabled for any real submit.
- Continue the guarded assignment experiment only with explicit bounds: keep low-confidence, unanswerable, low-score, max-question-cap, selected-state verification, manual pre-submit review, reviewed-answer title guarding, numeric text-key ordering, and visible-position text-control ordering checks enabled unless the user explicitly changes them.
- Run `.\run_chaoxing.ps1` for a live headed check, or set `CX_HEADLESS=true` / `CX_SLOW_MO_MS=0` for background operation.
- Use `.\run_chaoxing.ps1 --course "课程名称关键词"` to target the course to watch.
- For `“四史”专题课`, prefer `.\run_chaoxing.ps1 --course 四史 --headless --fast-actions --browser-channel chrome`; this avoids PowerShell smart-quote parsing issues while still matching the correct card.
- If Playwright Chromium is missing or downloading too slowly, use the installed browser path with `--browser-channel chrome`.
- Let the script continue beyond `--max-chapters 5` on `中国近现代史纲要` or another authorized course to validate longer unattended operation and full-course completion.
- Do not treat tutai as a current Chaoxing runner; its previous deployment and remote `.env` have been removed.
- If a suitable network path/proxy is configured on tutai, rerun `python -u main.py --course "中国近现代史纲要" --max-chapters 1 --headless --fast-actions` before any longer run.
- For now, use the local machine as the verified runner for `中国近现代史纲要`; local 5-task validation remains the strongest end-to-end evidence.
- Keep tutai on Playwright `1.29.1` / Chromium `109` while it remains Windows Server 2012 R2, unless the OS is upgraded and browser compatibility is revalidated.
- Continue course watcher validation locally for any future course or regression evidence.
- For `“四史”专题课`, course-list progress is now verified as `8/8 100%`. Future work can focus on committing/reviewing the watcher fixes or applying the same checks to another test course.
- During the temporary assignment tester experiment, keep runs explicit and bounded with environment variables; record any live behavior and restore the real-site hard-block when the experiment ends.
- For fastest assignment experiments, use the new `ASSIGNMENT_*_DELAY_SECONDS`, `ASSIGNMENT_SETTLE_WAIT_MS`, `ASSIGNMENT_NEXT_BUTTON_TIMEOUT_MS`, `ASSIGNMENT_CONFIRMATION_*`, and `ASSIGNMENT_BLOCK_ON_INCOMPLETE=false` controls.
- For direct Inbox scans, prefer `ASSIGNMENT_INBOX_URL=https://notice.chaoxing.com/pc/notice/myNotice?...`; the old `i.chaoxing.com/base?...` shell can leave the notice iframe unloaded.
- For assignment experiments, keep `ASSIGNMENT_AI_REVIEW_MULTIPLE=true` unless measuring raw first-pass model behavior.
- Keep `ASSIGNMENT_AI_ENABLE_IMAGES=true` for image-question experiments; lower `ASSIGNMENT_AI_MAX_IMAGE_BYTES` only if request size becomes a problem.
- Keep `ASSIGNMENT_STOP_ON_LOW_CONFIDENCE=true` for real experiment runs. Tune with `ASSIGNMENT_AI_MIN_CONFIDENCE`, `ASSIGNMENT_AI_REQUIRE_CONFIDENCE`, and `ASSIGNMENT_HOLD_BROWSER_ON_LOW_CONFIDENCE` only when deliberately measuring model behavior.
- Keep `ASSIGNMENT_STOP_ON_UNANSWERABLE=true` and `ASSIGNMENT_STOP_ON_LOW_SCORE=true` for real experiment runs; tune the low-score threshold with `ASSIGNMENT_MIN_ACCEPTABLE_SCORE`.
- For more complete assignment experiment runs, keep `ASSIGNMENT_AI_ENHANCED_REVIEW=true` and tune completion-vs-risk with `ASSIGNMENT_AI_REVIEW_SAMPLES`, `ASSIGNMENT_AI_CONSENSUS_RATIO`, and `ASSIGNMENT_RISK_BUDGET_POINTS` instead of simply lowering `ASSIGNMENT_AI_MIN_CONFIDENCE`.
- Configure `assignment_tester.py` selectors only against authorized pages.
- Keep live runs constrained with `ASSIGNMENT_MAX_SCAN_ROUNDS=1` and `ASSIGNMENT_MAX_CANDIDATES_PER_ROUND=1` when doing manual Inbox pressure tests.
- Use localhost or another controlled fixture to validate live AI answer selection, real clicking behavior, text `.fill()`, and submit confirmation behavior when possible.

## Blockers

- `.env` contains local credentials and is ignored by `.gitignore`; do not commit or share it.
- `“四史”专题课` 6.1 previously produced a false no-next completion and then a false incomplete result from static rule text; both paths are fixed locally. Course-wide completion beyond this verified task point should still be checked through the platform course list if needed.
- tutai deployment has been removed. Re-deploy only after deciding on a viable network path; the previous real course smoke was blocked by Chaoxing returning the self-looping `passport403.html` redirect from tutai's cloud-server network.
- tutai is Windows Server 2012 R2; current Playwright/Chromium releases are incompatible. Reinstalling unpinned `playwright` may upgrade back to an incompatible browser unless the runtime is pinned or the OS is upgraded.
- `assignment_tester.py` currently has a documented safety configuration drift: code defaults allow non-dry-run behavior and real-site AI/actions more broadly than the original staging-only design. This is accepted temporarily for the current experiment by user direction, but must be restored before returning to normal operation.
- Remote `glm/chat2api` is usable again after the 2026-06-06 token and session-cookie refresh. The raw `sessionToken` path is verified, so the daily `chat2api-session-refresh.timer` should be able to refresh while that session remains valid. The current installed access token expires on 2026-06-16 06:58:20 UTC; if the session is revoked or expires before the next refresh, provide a fresh access token or session payload.
- The latest parse-fix validation log is incomplete at the final submit-confirmation step because the shell command timed out while the script was handling submit. Do not claim final score or final completion for that 44-question candidate from this log alone.
- The 2026-06-03 run toward "no unsubmitted assignments" completed by its then-current experiment criteria, but it is no longer the latest assignment state after the 2026-06-06 retry/manual-review work.
- Image-question support is verified for transport and extraction, but not yet against a real Chaoxing image-heavy assignment with known answers.
- Confidence gating depends on the model's self-reported confidence, so it reduces obvious low-certainty submissions but is not a correctness proof.
- The local `ASSIGNMENT_INBOX_URL` still points at the `i.chaoxing.com` shell, but `assignment_tester.py` now extracts the current direct `notice.chaoxing.com` URL from the menu. If the shell menu layout changes, direct Inbox URL configuration may be needed again.
- Earlier assignment experiment incidents remain regression history: prior runs exposed Q27 low-confidence stops, transient AI/backend failures, login timeouts, and an old max-question-cap submit bug. Current code has AI retries, login retries, max-question sentinels, selected-state verification, and the manual-review gate, but these guardrails should stay enabled for future real runs.
- Current real scan evidence `logs/assignment_until_empty_final_20260606_190457.*` shows the scanned Inbox candidates are completed/closed or notice-only; no unsubmitted `dowork` page was reached in the latest run.
- Login credentials are stored only in local ignored `.env`; do not share or commit it.
- `assignment_tester.py` has no course fallback by design; if the direct `notice.chaoxing.com` Inbox URL or its `s=` token changes, update `ASSIGNMENT_INBOX_URL`.

## Validation Expectations

- Local syntax checks can pass immediately.
- Latest `2025-2026(2) 物理学` mixed-resource check on 2026-06-14 passed: a probe reproduced `5.1 物质的微观模型` as `1课件 / 2视频` with only the PDF card loaded initially; local Playwright fixture verified the resource-card traversal; live run `logs/course_physics_5_1_card_fix_20260614_163213.log` handled the courseware card, switched to `2视频`, watched the video to 100%, and exited 0 at the configured `--max-chapters 1` cap.
- Latest `道德与法治` course-watcher checks on 2026-06-14 passed: the initial full run stopped at `6.5` with true no-source evidence (`duration=0`, `networkState=3`) and screenshots under `logs/screenshots/20260614_103410_*`; after adding alternate-route recovery, `python -m py_compile main.py assignment_tester.py`, `python main.py --help`, `git diff --check`, and an offline Playwright route-switch fixture passed. Follow-up real audits found and completed residual items at `5.1`, `5.4`, `6.1`, and `6.5`; after the stricter end-state fix, `logs/course_morality_final_no_unfinished_audit_20260614_123408.log` exited 0 from a fresh session and reported no unfinished task points.
- Latest completed-task skip checks passed locally and live: `python -m py_compile main.py assignment_tester.py`, `python main.py --help`, `.\run_chaoxing.ps1 --help`, `git diff --check`, an offline Playwright DOM smoke, and live `道德与法治` checks. The first live run `logs/course_morality_skip_20260613_232951.out.log` processed one task point, skipped completed catalog entries from `1.1` through `4.2`, and advanced to the next unfinished item. The targeted retry `logs/course_morality_completed_chapter_skip_retry_20260613_234602.out.log` opened completed `4.2`, printed `当前任务点已在章节目录中标记完成，跳过视频播放`, advanced to `4.4`, and exited 0. An intermediate probe found Chaoxing marks the active catalog item with `poscatalog_active`; `main.py` now recognizes that active-state form.
- Latest `2026春-线性代数` course-watcher checks on 2026-06-09 passed: `python -m py_compile main.py assignment_tester.py`; a helper smoke verified `video_media_ready()` returns True for `networkState=1` and False for `networkState=3`; the live retry `logs/course_linear_retry_after_networkstate_fix_20260609_101800.out.log` logged login, course open, `2.5`, the first non-video task, task point 2 video 1/6 playing at 2x after the source-handling fix, and the final run completed normally with the no-next chapter list screenshot and `课程 [线性代数] 已处理到最后一节，自动看课流程完成。`
- Latest text-fill ordering regression on 2026-06-06 passed: a local Chrome Playwright fixture deliberately ordered hidden textareas as 10/11/12 before 1-9 while visible editor iframes were in 1-12 order; `find_text_answer_controls()` plus `apply_text_answers()` filled `answerEditor1..12` with `ans1..12` correctly. `python -m py_compile main.py assignment_tester.py` also passed.
- Latest doc-sync verification on 2026-06-06 passed: `python -m py_compile main.py assignment_tester.py`, `git diff --check` with LF/CRLF warnings only, `python main.py --help`, `.\run_chaoxing.ps1 --help`, and a targeted offline assignment parser/review smoke covering malformed JSON recovery, unusable confidence detection, reviewed-answer replay, and reviewed-answer title mismatch halt.
- Latest repository maintenance checks passed: `python -m py_compile main.py assignment_tester.py`, `git diff --check`, `python main.py --help`, `.\run_chaoxing.ps1 --help`, and a local course-keyword variant smoke for `“四史”专题课`. Ignored local runtime artifacts remain untracked: `.env`, `logs/`, `dist/`, and `__pycache__/`.
- Latest timing optimization checks passed: `python -m py_compile main.py assignment_tester.py`, `python main.py --help`, `.\run_chaoxing.ps1 --help`, course fast-action config override smoke, assignment fast default config smoke, and a local two-question assignment fixture that selected answers and submitted in 0.844s using system Chrome headless.
- GitHub sync checks passed: `git push -u origin main` succeeded after network recovery, local branch is clean and tracks `origin/main`, and remote tree contains only source/docs/script files, not `.env`, `logs/`, `dist/`, or caches.
- Latest `glm/chat2api` recovery checks passed on 2026-06-06: `/home/ubuntu/chat2api/data/token.txt` contains one valid JWT, `chat2api.service`, `cloudflared-api-tunnel.service`, `mihomo.service`, and `chat2api-session-refresh.timer` are active, local `http://127.0.0.1:5005/v1/chat/completions` returned `OK`, public `https://api.2070814.xyz/v1/chat/completions` returned `OK`, and local `assignment_tester.ask_ai_brain_decision('2+2=?')` returned `['B']` with confidence `1.0`. Observed transient upstream `403 cf_chl_opt` retries can occur before success. The refresh-script guard was tested with an invalid split cookie and preserved the valid JWT; the raw `sessionToken` refresh path then succeeded.
- Latest same-URL assignment result fix checks passed: `python -m py_compile assignment_tester.py main.py`, local submitted-result/low-score-marker smoke, `git diff --check`, and two real Inbox scans on 2026-06-06. The final UTF-8 log `logs/assignment_real_until_empty_20260606_163727.err.log` shows result-page detection and a low-score/retry guard halt, with no `RuntimeError` or traceback.
- Latest malformed AI JSON fallback checks passed: targeted local smoke verified malformed JSON with a recoverable `"answer"` returns the answer as low-confidence, valid JSON with malformed confidence string returns the answer as low-confidence, and normal confidence remains accepted; `python -m py_compile main.py assignment_tester.py` also passed.
- Latest manual-submit evidence on 2026-06-06: visible-correct retry candidate submitted with `100/100`; biology 作业11 submitted with `96/100` after manual correction and selected-state verification; biology 作业10 submitted with `95.5/100` after manual correction of Q83 and reviewed-answer-file selected-state verification.
- Latest no-unsubmitted scan evidence on 2026-06-06: `logs/assignment_goal_audit_20260606_191447.*` exited with code 0 after 10 candidates, 9 `completed_or_closed`, 1 `notice_only`, no new generated review report, no candidate limit, and the `ASSIGNMENT_EXIT_WHEN_NO_UNSUBMITTED=true` stop condition.
- Latest real submit-enabled scan passed operationally on 2026-06-02: login and Inbox scan succeeded, one 10-question `dowork` assignment was answered and submitted, and the run halted before submit on the next English image/fill-in assignment when no usable AI answer was available.
- Latest parser-fix checks passed: `python -m py_compile main.py assignment_tester.py`; local noisy/fenced JSON parsing smoke; local `"6"` to option `C` mapping smoke; and real log evidence that a fenced ` ```web ... ``` ` response was parsed into a usable `['A']` answer.
- Latest bounded real parse-fix scan passed the original bug path operationally but not the full end-to-end final result: it selected/mapped 44 questions and entered submit confirmation handling, then the shell command timed out before final score/result logging.
- Latest high-risk review and UEditor sync checks passed: `python -m py_compile main.py assignment_tester.py`, targeted local smoke for noisy JSON parsing/option-value mapping/high-risk consensus/media labels, live AI probe showing the previous Q3 high-confidence disagreement is now halted by high-risk consensus, and local hidden UEditor sync smoke using system Chrome. A first smoke attempt with Playwright-managed Chromium failed because that browser binary is missing locally; rerun browser smokes with `channel='chrome'` unless Playwright browsers are installed.
- Latest incident-regression checks passed: read-only live result inspection confirmed biology 作业9 is blank/0 and English Unit 5 Q2/Q4 are 0; local Chrome smokes verified hidden text controls are ordered by visible editor position, choice clicks must produce a selected state, a non-state-changing click is rejected, high-risk text adjudication cannot override review disagreement, and `python -m py_compile main.py assignment_tester.py` passes.
- Latest full guarded assignment run passed: `logs/assignment_real_until_empty_20260603_000824.log` processed all 8 scanned Inbox candidates, classified 7 as completed/closed and 1 as notice-only, and exited only after all scanned assignment candidates were completed/closed or notice-only. A final `python -m py_compile main.py assignment_tester.py` also passed.
- Latest `“四史”专题课` course-entry checks passed: `python -m py_compile main.py assignment_tester.py`; a local selector regression confirmed `.course-info a.color1` is chosen over a broad container; a live bounded entry probe logged in, matched `.course-info a.color1`, opened the course page, and found the chapters tab; and `.\run_chaoxing.ps1 --course 四史 --chapter "6.1" --max-chapters 0 --headless --fast-actions --browser-channel chrome` clicked `6.1`, detected learning content, and exited before processing videos.
- Earlier assignment AI reliability checks passed before the current token outage: `python -m py_compile main.py assignment_tester.py`, local answer-normalization smoke, and a mocked multiple-choice review smoke where a first-pass `ABC` answer was corrected to `ABCD`.
- Earlier image-question checks passed before the current token outage: local Playwright fixture captured one question-container PNG and built a multimodal payload, and `ask_ai_brain()` returned `["A"]` through `gpt-5.5` with a data URL image attached.
- Earlier confidence-gate checks passed before the current token outage: local mock tests covered high-confidence continue, low-confidence halt, missing-confidence halt, and opt-out behavior; live `gpt-5.5` returned `{"answer":["B"],"confidence":1}` for a deterministic arithmetic smoke.
- Latest guarded live scan passed operationally: login succeeded, Inbox opened, 0 assignment candidates were found, and the monitor exited at `ASSIGNMENT_MAX_SCAN_ROUNDS=1` without answering or submitting.
- Latest fixed guarded run passed the intended stop condition: the scanner found 9 candidates and halted before submit when question 27 returned confidence 0.68.
- Shared-option checks passed on the live assignment: Q53/Q54 are now parsed as `shared_options`, and answer order is preserved instead of being sorted.
- Latest enhanced-review/cap checks passed locally: `python -m py_compile main.py assignment_tester.py`, answer normalization, shared-option answer order preservation, mock 2-of-3 consensus acceptance with risk accounting, missing-confidence halt, risk-budget submit block, transient AI request retry, score parsing, and max-question-cap submit prevention with a `max_questions + 1` sentinel. A live retry before AI request retries were added reached Q45 and stopped on a transient API `ProxyError`.
- Latest guarded dry-run checks passed: `python -m py_compile main.py assignment_tester.py`, direct dry-run shared-option answer generation smoke, and real-site Inbox scan with AI/submission disabled that mapped `dowork` question controls without submitting.
- End-of-video next-task handling is verified on `中国近现代史纲要` for 8 videos across 5 task points, including same-URL transitions and an onclick fallback after overlay interception.
- Full-course completion is now verified on `2026-06-09`.
- Local course-watcher checks passed: syntax check, env config parsing, and simulated `#prevNextFocusNext` URL-change smoke test.
- Latest course-watcher CLI checks passed: syntax check, direct help output, launcher help forwarding, argument override parsing, and invalid headless/headed conflict handling.
- Latest course-watcher false-completion regression checks passed locally: no-next plus unfinished marker returns failure, and main-page `<video>` elements are detected by `video_locators()`. Live full-course completion is now verified on `2026-06-09`.
- Latest `“四史”专题课` 6.1 live checks passed: `python -m py_compile main.py assignment_tester.py`; live run log `logs/course_sishi_continue_20260603_104033.out.log` shows video progress from 58.5% to 100%; screenshot `logs/screenshots/20260603_110216_no_next_but_incomplete.png` shows platform `任务点已完成` and a green 6.1 check; follow-up probe returned `signals=[]` and `no_next_result='completed'`; final course-list probe showed `任务点进度: 8/8 100%`.
- Latest OpenClaw-triggered runner fix checks passed: `python -m py_compile main.py assignment_tester.py` and `python main.py --help`.
- Latest OpenClaw-triggered live check passed the previous stale-locator failure point and was intentionally stopped at a 7238s video; this validates the re-located video progress read for the old failure path but does not validate full-course completion.
- tutai remote checks passed on 2026-04-27 after downgrading to Playwright `1.29.1`: syntax check, direct help output, and headless Chromium browser smoke.
- tutai real course smoke was attempted on 2026-04-27 and failed at login navigation because Chaoxing redirected tutai to `passport403.html`; no course page or video was reached.
- tutai cleanup verification passed: `D:\services\course-rpa-node` no longer exists, and the prior upload path `D:\qampp\htdocs\course-rpa-node-tutai-20260427.zip` also does not exist.
- Assignment tester now has one live dry-run Inbox-to-dowork validation against a read Chaoxing notice; no answer was submitted.
- Live API connectivity check now passes; fake API parsing tests for choice and text answers pass; local submit confirmation fixtures pass.
- Latest assignment submit checks passed locally: `python -m py_compile main.py assignment_tester.py`, native `confirm()` auto-accept fixture, and custom HTML `.bluebtn` confirmation fixture.
- Latest multi-step submit checks passed locally: native dialog does not re-click the main submit button, custom `确定` confirmation works, custom popup `提交` confirmation works, and a two-step `确定` then `提交` flow works.
- Monitor mode is live-verified for one pure Inbox scan round after login navigation retry; long-running hourly operation was not left running.
- Course next-chapter transition is integration-verified without waiting for another full video completion.

## Active Files

- `main.py`
- `assignment_tester.py`
- `run_chaoxing.ps1`
- `.env` (local ignored credentials)
- `docs/STATUS.md`
- `docs/TASKS.md`
- `docs/DECISIONS.md`
- `README.md`
- `.gitignore`
- `docs/TASKS.md`
- `docs/STATUS.md`
- `docs/DECISIONS.md`
